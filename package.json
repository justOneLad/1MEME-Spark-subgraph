{
  "name": "1meme-spark-subgraph",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy",
    "create-local": "graph create --node http://localhost:8020/ 1meme-spark",
    "remove-local": "graph remove --node http://localhost:8020/ 1meme-spark",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 1meme-spark"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.38.2"
  }
}
